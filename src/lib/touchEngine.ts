export const defaultOffsets=[0,2,6,11,18];export function addDays(d:Date,n:number){const x=new Date(d);x.setDate(x.getDate()+n);return x}export function toYMD(d:Date){const y=d.getFullYear();const m=String(d.getMonth()+1).padStart(2,"0");const dd=String(d.getDate()).padStart(2,"0");return `${y}-${m}-${dd}`}export function generateTouchNames(){return["Touch 1 — Intro email / LinkedIn","Touch 2 — Follow-up email / social proof","Touch 3 — Call / voicemail","Touch 4 — Insight share / referral","Touch 5 — Break-up / last chance"];}